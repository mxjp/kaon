{
	"name": "kaon",
	"type": "module",
	"version": "0.1.0",
	"main": "./kaon.js",
	"scripts": {
		"minify": "terser --compress --mangle --toplevel --output kaon.min.js -- kaon.js",
		"compress": "node scripts/compress.js",
		"build": "npm run minify && npm run compress"
	},
	"devDependencies": {
		"terser": "^5.39.0"
	}
}
